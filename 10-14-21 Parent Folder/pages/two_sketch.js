var nums = [100, 25, 46, 72, 35, 28, 99, 33, 25, 66, 74, 82];
let r, g, b

function setup() {
  createCanvas(500, 400);
}

function draw() {
  background(220);
  
  stroke(255, 0, 0)
  strokeWeight(10)
  line(144, 0, 144, 400)
  
  if (mouseX > 150)
  
  for (var i = 0; i < 11; i++) {
  r = random(255,)
  g = random(255,)
  b = random(255,)
  stroke(0, 33);
  fill(r, g, b, 80)
    ellipse(300, 200, nums[i], nums[i]);
    
  }
  
  
}

